{
  "name": "carlovinci-com-au",
  "version": "1.0.0",
  "private": true,
  "description": "Source for carlovinci.com.au — Carlo Vinci's personal portfolio site.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:css": "stylelint \"**/*.css\"",
    "lint:html": "htmlhint \"*.html\"",
    "lint": "npm run lint:css && npm run lint:html"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "prettier": "^3.3.3",
    "stylelint": "^16.9.0",
    "stylelint-config-standard": "^36.0.1"
  }
}
